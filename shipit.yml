ci:
  require:
    - 'CI / Ruby 2.4 Gemfile (push)'
    - 'CI / Ruby 2.5 Gemfile (push)'
    - 'CI / Ruby 2.6 Gemfile (push)'
    - 'CI / Ruby 2.7 Gemfile (push)'
deploy:
  override:
    - bundle exec rake build
    - bundle exec package_cloud push shopify/gems pkg/*.gem
